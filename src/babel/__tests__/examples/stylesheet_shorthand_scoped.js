import * as zacs from '@nozbe/zacs'

const styles = zacs.stylesheet({
  scoped: {
    web: {
      margin: [5, 10],
      border: [1, 'solid', 'red'],
    },
    native: {
      margin: [5, 10],
      border: [1, 'solid', 'red'],
    },
  },
})
