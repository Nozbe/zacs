/* eslint-disable */
import zacs from 'zacs'

const styles = zacs._experimentalStyleSheet({
  root: {
    backgroundColor: 'red',
    height: 50,
    width: '100%',
    flex: 1,
    zIndex: 1000,
    WebkitPaddingStart: 20,
  },
})
